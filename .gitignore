# macOS Finder metadata — the reference/ material was gathered on a Mac.
.DS_Store
._*

# Backups written by overrides/apply.sh and by Omarchy's own config edits.
*.pre-cllpse
*.bak
*.bak.*

# Regenerated by overrides/apply.sh on every run -- never hand-edited.
overrides/hypr/keybind-unbinds.lua
overrides/hypr/keybind-current.conf

# Editor / OS scratch
*.swp
*~

# Python bytecode — overrides/chromium/default-zoom.py is imported by its tests.
__pycache__/
*.pyc
