# bat

One line: point bat at the terminal's ANSI palette instead of its own bundled theme.

The script is [`bat.sh`](bat.sh). It is runnable on its own and is also
called by [`../apply.sh`](../apply.sh), which owns the order. Numbered
sections below match the `# See README.md (n)` pointers in that script.
