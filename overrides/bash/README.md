# bash

fzf, lsd colours and the tool aliases, fenced into ~/.bashrc.

The script is [`bash.sh`](bash.sh). It is runnable on its own and is also
called by [`../apply.sh`](../apply.sh), which owns the order. Numbered
sections below match the `# See README.md (n)` pointers in that script.
