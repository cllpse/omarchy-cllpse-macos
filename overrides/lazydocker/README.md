# lazydocker

The same four gocui theme keys as lazygit, same ANSI vocabulary.

The script is [`lazydocker.sh`](lazydocker.sh). It is runnable on its own and is also
called by [`../apply.sh`](../apply.sh), which owns the order. Numbered
sections below match the `# See README.md (n)` pointers in that script.
